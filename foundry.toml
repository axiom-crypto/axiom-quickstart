[profile.default]
src = "src"
out = "out"
libs = ["node_modules", "lib"]
ffi = true
fs_permissions = [{ access = "read", path = "./data" }]

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
[rpc_endpoints]
goerli = "${PROVIDER_URI_GOERLI}"
